import { createIconSetFromIcoMoon } from '@expo/vector-icons';
import fontConfig from '../../assets/fonts/Instabyte/selection.json';

const Instafont = createIconSetFromIcoMoon(fontConfig);

export default Instafont;
